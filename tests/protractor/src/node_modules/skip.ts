// this file should not be converted

import { $$, ElementFinder } from 'protractor';

export const notConverted = () => $$('.a');

export const veryUsefulClickUtility = (element: ElementFinder) => {
    element.click();
}
